{
  "compilerOptions": {
    /* Basic Options */
    "target": "ES2016",                       // Compila para uma versão moderna do JS compatível com a maioria dos ambientes Node.js.
    "module": "commonjs",                     // Usa o sistema de módulos padrão do Node.js.
    "rootDir": "./src",                       // Onde nosso código TypeScript de origem vive.
    "outDir": "./dist",                       // Onde o código JavaScript compilado será colocado.

    /* Strict Type-Checking Options */
    "strict": true,                           // Habilita todas as checagens de tipo estritas. Essencial para código robusto.
    "noImplicitAny": true,                    // Exige que todas as variáveis tenham um tipo definido.

    /* Module Resolution Options */
    "esModuleInterop": true,                  // Melhora a compatibilidade entre módulos CommonJS e ES Modules.
    "forceConsistentCasingInFileNames": true, // Garante que os nomes dos arquivos sejam consistentes em 'case'.

    /* Advanced Options */
    "skipLibCheck": true,                     // Pula a verificação de tipos em arquivos de declaração (.d.ts) das dependências.
    "resolveJsonModule": true                 // Permite importar arquivos .json como módulos.
  },
  "include": ["src/**/*"],                    // Define que apenas os arquivos dentro da pasta 'src' devem ser compilados.
  "exclude": ["node_modules", "**/*.spec.ts"] // Exclui a pasta de dependências e arquivos de teste da compilação.
}