{
  "name": "checker-worker",
  "version": "1.0.0",
  "description": "Background worker to check endpoint health.",
  "main": "dist/worker.js",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node-dev --respawn --transpile-only src/worker.ts",
    "start": "node dist/worker.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "AgnesMillie",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.5",
    "mysql2": "^3.10.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "@types/node-fetch": "^2.6.11",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.5"
  }
}